# values.yaml for loki-stack
loki:
  enabled: true
  persistence:
    enabled: true
    size: 10Gi
  service:
    type: ClusterIP
    port: 3100

promtail:
  enabled: false

grafana:
  enabled: false
